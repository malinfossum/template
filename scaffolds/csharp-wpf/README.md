# csharp-wpf (in progress)

WPF/MVVM starter. Currently editor configs only (`.editorconfig`, `.gitignore`) — the MVVM skeleton and `tokens.xaml` (design-system palette, spacing, and typography mirrored for XAML) are planned; see `docs/specs/`.
