/* ======================================================================
   src/utils/dom.js — OPTIONAL DOM HELPERS
   Empty until a project needs shared helpers; delete the folder if
   nothing imports it.
   ====================================================================== */
