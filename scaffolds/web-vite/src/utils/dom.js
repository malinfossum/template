/* ======================================================================
   src/utils/dom.js — OPTIONAL UTILITIES
   OK TO KEEP BLANK ✅
   - If you don't use it in an assignment, it will NOT affect anything.
   - You can delete utils/ safely if nothing imports it.
   ====================================================================== */

// Leave empty until you need helpers.
// Example (later):
// export const qs = (root, selector) => root.querySelector(selector);
