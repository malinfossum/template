/* ======================================================================
   vite.config.js
   OPTIONAL
   - Only needed when deploying (e.g., GitHub Pages base path).
   ====================================================================== */

import { defineConfig } from "vite"

export default defineConfig({
	// If you deploy to GitHub Pages under a repo name, set:
	// base: '/your-repo-name/',
})
