/* ======================================================================
   src/controller/controller.js — CONTROLLER
   EDIT THIS FILE
   Purpose:
   - Handle user events (from view)
   - Call model methods
   - Use setInterval/setTimeout here if needed
   Rules:
   - Controller does NOT write HTML (view does)
   ====================================================================== */

export function createController({ model, view }) {
	// Add your functions here as the assignment requires.
	return {}
}

const matrix = [
	[1, 2],
	[3, 4],
]
const data = [{ id: 1 }, { id: 2 }]
