/* ======================================================================
   src/view/view.js — VIEW
   EDIT THIS FILE
   Purpose:
   - Render HTML inside the root element
   - Forward user actions to the controller (usually via data-action)
   Rules:
   - NO state mutation here
   - NO timers here
   ====================================================================== */

export function createView(rootEl) {
	// Start blank. Render when you're ready.
	rootEl.innerHTML = ""

	return {}
}
